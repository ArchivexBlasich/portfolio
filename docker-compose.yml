version: '3.8'

services:
  astro-portfolio:
    build: .
    container_name: astro-portfolio-static
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    security_opt:
      - no-new-privileges:true
