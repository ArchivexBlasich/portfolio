---
import SocialPill from "./SocialPill.astro";
import { t } from "../../i18n/ui";

interface Props {
  locale: "en" | "es";
  srStartIndex?: number;
}

const { locale, srStartIndex = 0 } = Astro.props;
---

<div class="flex flex-wrap items-center justify-center gap-4">
  <SocialPill
    href="https://github.com/ArchivexBlasich"
    size="size-8"
    svgRef="#github_dark"
    aria_label={t("social.github.aria", locale)}
    srIndex={srStartIndex}
    data-scroll-reveal
  >
    {t("social.github", locale)}
  </SocialPill>

  <SocialPill
    href="https://www.linkedin.com/in/fabricio-lucas-blasich"
    size="size-8"
    svgRef="#linkedin"
    aria_label={t("social.linkedin.aria", locale)}
    srIndex={srStartIndex + 1}
    data-scroll-reveal
  >
    {t("social.linkedin", locale)}
  </SocialPill>

  <SocialPill
    href="mailto:fblasich0@gmail.com"
    size="size-8"
    svgRef="#message-2-share"
    aria_label={t("social.mail.aria", locale)}
    srIndex={srStartIndex + 2}
    data-scroll-reveal
  >
    fblasich0@gmail.com
  </SocialPill>
</div>
