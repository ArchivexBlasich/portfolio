---
import Main from "../components/main/Main.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { t } from "../i18n/ui";

const locale = "en";
---

<BaseLayout title={t("site.title", locale)} locale={locale}>
  <Main locale={locale} />
</BaseLayout>
